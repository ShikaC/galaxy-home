# Dogfood 摩擦清单

只记真实使用中的卡点，不在此扩需求范围。条目解决后可标「已缓解」并指向提交；未提交前指向当前工作树与验收证据。

## 进行中

| 日期 | 摩擦 | 备注 |
|------|------|------|
| 2026-08-09 | 真实模型偶发省略操作块 | 提示词补充强制操作块与唯一活跃项优先；服务端对无操作块的成功声明保持无写入。AI 上下文已补充 `active/completed/archived` 状态，最终保守/开放各 12/12；模型完全省略操作块仍保留重复复跑作为残余风险 |

## 已缓解

| 日期 | 摩擦 | 处理 |
|------|------|------|
| 2026-08-10 | 模型将 JSON 动作围栏写成大小写或空格变体 | `extractChatActions` 兼容 `json` 标签大小写与标签前空格，并保留围栏后的解释文字；新增集成回归测试（当前工作树，待提交） |
| 2026-08-07 | 侧栏多操作仍依赖模型稳定输出数组 | AI 动作协议支持同批执行、别名引用、部分成功说明；新增分类创建/关联、习惯打卡动作与无操作块成功声明拦截；[真实模型复跑](./ai-oral-script.md) 修复后保守/开放各 12/12（当前工作树，待提交） |
| 2026-08-08 | 768px 下部分页面主内容内部裁切 | 900px 断点收拢首页引导、习惯/回顾说明、设置布局，并为页面头/主区补 `min-inline-size: 0`；[人工 QA 记录](./manual-qa-2026-08-08.md) 与 `.omo/evidence/friction-fix-20260808/` 已复验（当前工作树，待提交） |
| 2026-08-08 | 桌面 WebView 启动/刷新期间出现空白等待 | Tauri 等待 Web/API 就绪，HTML 首屏提供静态连接态，React 接管前后均有可见反馈；启动/重载时序见 `.omo/evidence/friction-fix-20260808/desktop-sequence-20260808.log`（当前工作树，待提交） |
| 2026-08-08 | 回收站恢复/永久删除缺少 pending 与行内结果反馈 | IconButton 增加 loading/禁用态，数据设置页增加成功/失败 Toast，并阻止慢请求重复点击；见 `.omo/evidence/friction-fix-20260808/web-trash-restored-1280.png`（当前工作树，待提交） |
| 2026-08-08 | 完成条目后从当前列表消失，不容易找到去处 | 首页与待办页完成后提示「查看已完成」，并支持直接重新打开；见 `.omo/evidence/friction-fix-20260808/web-home-1280.png`、`web-completed-1280.png`（当前工作树，待提交） |
| 2026-08-10 | 分类视图中完成条目后切换「已完成」被路由副作用重置到收集箱 | `TodosPage` 切换视图时保留 `view` 查询参数；新增路径已由 compact/wide 共 4 次 E2E 覆盖，完整 28/28 通过（当前工作树，待提交） |
| 2026-08-10 | Tauri 打包桌面端启动后立即退出 | release 资源根改为 `Contents/Resources/resources/app`，并复制 `db/migrations`；重新打包后原生窗口与 Node 服务均可启动，见 [桌面验收记录](./acceptance-21.md)（当前工作树，待提交） |
| 2026-08-10 | Tauri release 首次打开时先露出空白窗口 | `main` 窗口等待 `PageLoadEvent::Finished` 后再显示；重建 `.app` 后首帧直接为首页，并复验侧栏与 AI 面板（当前工作树，待提交） |
| 2026-08-07 | 批量中途失败不说清 | 报告已成功步数与失败步 |
| 2026-08-07 | 超今日 3 条主要额度半写入 / 整批失败 | AI 批次经 `fitTodayActions` 将超额主位/重点降为次要后继续执行；手动 UI 仍硬限额 409 |
| 2026-08-07 | 语音未配置仍可点录音 | 禁用并提示配置 |
| 2026-08-07 | 提醒像系统闹钟 | 设置页明确进程边界 |
| 2026-08-07 | 导出是否含 Key 不清 | 数据设置说明不含 secrets |
| 2026-08-07 | 刷新会话滚到顶部 | 消息列表哨兵滚动 |
| 2026-08-07 | 用户气泡选中色与背景同色 | 选中高亮对比 |
| 2026-08-07 | 窄屏隐藏「分类」标题 | 仅隐藏「视图」 |
| 2026-08-07 | 习惯卡编辑操作换行过高 | 与计数同一行 |
| 2026-08-07 | 随手记按钮图标与文字换行 | Button 去掉多余 span |
| 2026-08-07 | 今日误触完成后无法还原 | 首页已完成折区改为 `TaskRow` 可重新打开 |
| 2026-08-07 | 折叠导航无障碍名称 | 主导航 `aria-label`；装饰图标 `aria-hidden` |
| 2026-08-07 | 提醒 e2e 仅宽屏 | compact 同步 `GALAXY_CLOCK_NOW`，取消 skip |
| 2026-08-07 | 休息日仍可 API 打卡 | `recordHabit` / 未修正 `setHabitLog` 拒绝休息日 |
| 2026-08-07 | 改时区后列表不刷新 | 个人设置保存后使 items/habits/gains 等失效并提示日界 |
| 2026-08-07 | 昨日复盘主位满无法安排 | 增加「加入临时小事」 |
| 2026-08-07 | 完成勾选命中区过小 / 菜单焦点被掐 | 勾选 ≥36px；任务菜单保留 `:focus-visible` |
